import React from 'react';
import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({});

const SurveyPage = () => {
  const classes = useStyles();

  return <div>This is the survey page</div>;
};

export default SurveyPage;
